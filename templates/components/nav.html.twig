<nav class="navbar navbar-expand-lg navbar-light bg-primary">
  <div class="container d-flex justify-content-between">
    <p class="navbar-brand"><a class="nav-link" href="{{ path('index') }}">Flashcards Project</a></p>
    {# <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> #}
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav d-flex justify-content-between">
        {% if app.user %}
          <li class="nav-item {{ 'deckboard' in app.request.uri ? 'active' : '' }}">
            <a class="nav-link" href="{{ path('deckboard') }}">Deckboard</a>
          </li>
          <li class="nav-item {{ 'rechercher' in app.request.uri ? 'active' : '' }}">
            <a class="nav-link" href="{{ path('rechercher') }}">Rechercher un deck externe</a>
          </li>
            {% if "ROLE_ADMIN" in app.user.roles %}
            <li class="nav-item">
              <a class="nav-link" href="{{ path('admin-dashboard') }}">Admin Dashboard</a>
            </li>
          {% endif %}
        {% endif %}
      
        {% if not app.user %}
          <li class="nav-item {{ 'login' in app.request.uri ? 'active' : '' }}">
            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
          </li>
              {% if app.user.image is not null %}
                <p style="width:80% ; height:80%" class="m-0 p-0">
                  <img
                    src="/uploads/images/{{ app.user.image }}"
                    class="img avatar rounded"
                    width="70px"
                    height="auto"
                  />
                </p>
              {% else %}
                <p style="width:80% ; height:80%" class="m-0 p-0">
                  <img
                    src="/uploads/images/lapin.jpg"
                    class="img avatar rounded"
                    width="80px"
                    height="auto"
                  />
                </p>
              {% endif %}
        {% endif %}
      </ul>
    </div>
  </div> {# Fin container #}
</nav>