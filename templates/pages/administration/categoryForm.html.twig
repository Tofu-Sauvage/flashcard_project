{% extends './pages/administration/admin.base.html.twig'%} {%block body%}

<div class="content">
  <div class="container-fluid">
    <div class="card card-stats">
      <div class="card-header card-header-success card-header-icon">
        <div class="row">
          <div class="col-lg-1 col-md-1">
            <div class="card-icon primary">
              <i class="material-icons">article</i>
            </div>
          </div>
          <div class="col-lg-11 col-md-11">
            <p class="h2" style="color: white; text-align: left">Catégorie</p>
          </div>
        </div>
      </div>
      {{ form_start(categoryForm) }}
      <form>
        <div class="form-group container">
          <fieldset>
            <div class="form-group">
              {{ form_label(categoryForm.name) }}
              {{form_widget(categoryForm.name, {attr: {'class' :"form-control"




              }})}}
            </div>

            <div class="form-group">
              {{form_widget(categoryForm.add,{ 'label': 
              'edit' in  app.request.uri ? 'Mofifier':'Ajouter' 
           }
                )}}
            </div>
          </fieldset>
        </div>
      </form>
      {{ form_end(categoryForm) }}
    </div>
  </div>
</div>

{% endblock body %}
