{% extends './pages/administration/admin.base.html.twig'%} {%block body%}

<div class="content">
  <div class="container-fluid">
    <div class="col-lg-6 col-md-12">
      <div class="card">
        <div class="card card-profile">
          <div class="card-avatar">
            <a href="#pablo">
              <img class="img" src="/assets/img/faces/user-blanck.png" />
            </a>
          </div>
          <div class="card-body">
            <h6 class="card-category">id - {{ card.id }}</h6>
            <h4 class="card-title">Autheur - {{ card.author.name }}</h4>

            <div class="row">
              <div class="col-4">Question</div>
              <div class="col-8">{{ card.question }}</div>
            </div>
            <div class="row">
              <div class="col-lg-4">Réponse</div>
              <div class="col-lg-8 text-align-left">{{ card.answer }}</div>
            </div>
<p>
    
</p>
            <a href="#pablo" class="btn btn-primary btn-round">Supprimer</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock body%}
